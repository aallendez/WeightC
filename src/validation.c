#include <stdio.h>
#include "validation.h"

int validateEmail(const char *email) {
    // Code for validating email format
}

int validatePhoneNumber(const char *phoneNumber) {
    // Code for validating phone number format
}
